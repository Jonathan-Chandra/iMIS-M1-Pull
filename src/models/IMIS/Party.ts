//import type { IParty } from "./Interfaces/IParty";

/**
 * Concrete class stub representing an iMIS Party (person or organization).
 *
 * This class is currently a placeholder. The active type definition for party
 * data is the {@link IParty} interface in `./Interfaces/IParty.d.ts`, which
 * mirrors the iMIS `PersonData` contract returned by the REST API.
 *
 * Implement this class if domain logic (e.g. computed display names, address
 * formatting) needs to be encapsulated alongside the party data.
 */
export default class Party {


}