/**
 * M1 code indicating the current status of a member's NAR membership.
 * - `"A"` — Active
 * - `"I"` — Inactive
 * - `"T"` — Terminated
 * - `"P"` — Pending
 * - `"X"` — Expelled
 * - `"S"` — Suspended
 */
export type MemberStatusCode = "A" | "I" | "T" | "P" | "X" | "S";