/**
 * M1's string representation of a boolean flag. Used throughout the {@link Member}
 * interface in place of native TypeScript `boolean`.
 * - `"Y"` — true / yes / enabled
 * - `"N"` — false / no / disabled
 */
export type M1Boolean = "Y" | "N";