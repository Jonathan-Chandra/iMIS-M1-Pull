/**
 * M1 code representing the type of a phone number.
 * - `"O"` — Office
 * - `"H"` — Home
 * - `"DD"` — Direct Dial
 * - `"C"` — Cell / Mobile
 * - `"PF"` — Primary Fax
 */
export type PhoneTypeCode = "O" | "H" | "DD" | "C" | "PF";