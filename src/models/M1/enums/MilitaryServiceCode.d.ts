/**
 * M1 code indicating a member's military service status.
 * - `"VET"` — Veteran
 * - `"ACTDTY"` — Active Duty
 * - `"RESRV"` — Reserve
 * - `"RESVSP"` — Reserve Spouse
 */
export type MilitaryServiceCode = "VET" | "ACTDTY" | "RESRV" | "RESVSP";