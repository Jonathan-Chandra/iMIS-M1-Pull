/**
 * M1 code representing the category of an email address.
 * - `"P"` — Personal
 * - `"T"` — Third-party / temporary
 * - `"B"` — Business
 * - `"S"` — Secondary
 */
export type EmailTypeCode = "P" | "T" | "B" | "S";